#!/usr/bin/env bash
# Local "CI on push" without a hosted CI provider. Enable with:
#   git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec bash scripts/ci.sh
